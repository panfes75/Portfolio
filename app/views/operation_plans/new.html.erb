<div class="center">
  <%= form_with model: [@impulse_purchase, @operation_plan] do |f| %>
    <%= render 'layouts/error_messages', model: f.object %>
    <div class="mb-3">
      総稼働時間
      <div id="time", data-json="<%= @impulse_purchase.operating_time %>">
        <%= @impulse_purchase.operating_time %>
      </div>
    </div>
    <div class="mb-3">
      <div class="item-list">
        <div class="item">
        <%= f.label :monday_work_time, "1日の稼働時間" %>
        <%= f.number_field :monday_work_time, name: "mon", placeholder: "分単位で入力" %>
      </div>
      <input type="button" class="calc-btn" onclick="calc(mon.value);" value="計算">
      <hr class="line">
      <h1>結果</h1>
      <div class="mb-3">
        合計
        <%= f.number_field "#", id: "sum", readonly: "", size: "20x10" %> 分
      </div>
    </dev>
  <% end %>
</div>

<script>
  function calc(mon) {
    const sum_value = document.getElementById('sum').value 
                    = parseFloat((time).toFixed(2));
  }
</script>